{% extends 'base.html.twig' %}

{% block title %}Paiement réussi{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center">Paiement réussi</h1>
        <p class="text-center">Votre achat du cours "{{ course.name }}" a été validé avec succès.</p>
        <a href="{{ path('course_show', { 'id': course.id }) }}" class="btn btn-success">Voir le Cours</a>
    </div>
{% endblock %}